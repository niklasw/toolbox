#%Module 1.0
#
#  OpenMPI module for use with 'environment-modules' package:
#

set softhome		"/home/software"
set name		"ParaView"
set version		"5.6.0"
set prefix		"${softhome}/${name}/${version}-osmesa"

prepend-path 		PATH	"${prefix}/bin"
#prepend-path 		LD_LIBRARY_PATH "${prefix}/lib"

